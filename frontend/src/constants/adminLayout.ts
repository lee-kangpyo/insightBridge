/**
 * `/admin/*` 페이지 본문 래퍼 — 좌우·상하 패딩은 `MainLayout`의 `<main>`에서 통일.
 * 여기서는 폭·플렉스·스크롤 영역만 맞춘다.
 */
export const ADMIN_PAGE_CONTAINER_CLASS =
  'max-w-[1600px] mx-auto w-full flex flex-col gap-8 h-full min-h-0';
